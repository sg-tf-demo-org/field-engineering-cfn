AWSTemplateFormatVersion: "2010-09-09"
Description: Direct GitLab PR bypass — weak S3 for step-3 governance scan (expect FAIL) [1784637558]

Resources:
  BypassPublicBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub aiden-bypass-public-${AWS::AccountId}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false
